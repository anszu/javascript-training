// isPrime - Returns true or false, indicating whether the given number is prime.

const isPrime = (numb) => {
    const bool = false;
    console.log(numb);
    return (bool);
};

module.exports = isPrime;
