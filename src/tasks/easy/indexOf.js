// indexOf - Implement the indexOf function for arrays.

const indexOf = (arr, element) => {
    const index = 0;
    console.log(arr);
    console.log(element);
    return (index);
};

module.exports = indexOf;
