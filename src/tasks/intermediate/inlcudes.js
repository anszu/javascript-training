// includes - Return true or false indicating whether the given number appears
// in the given sorted array. Can you do it in O(log(N)) time?

const includes = (arr, number) => {
    const bool = false;
    console.log(arr);
    console.log(number);
    return (bool);
};

module.exports = includes;
