// fib2 - Like the fib function you implemented above,
// able to handle numbers up to 50 (hint: look up memoization).

const fib2 = (numb) => {
    console.log(numb);
    return (numb);
};

module.exports = fib2;
